import { SortBedragenByDatePipePipe } from './sort-bedragen-by-date-pipe.pipe';

describe('SortBedragenByDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SortBedragenByDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
