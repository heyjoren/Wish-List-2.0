import { SortItemsByDatePipe } from './sort-items-by-date.pipe';

describe('SortItemsByDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SortItemsByDatePipe();
    expect(pipe).toBeTruthy();
  });
});
